var source = './src/';
var build = './build/';
var compile = './compile/';
var root = './';
var index = 'index.html';
var temp = './temp/';
var gulp_dir = './Gulp';

module.exports = {
    source: source,
    gulp_dir: gulp_dir
};